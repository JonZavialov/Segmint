# Segmint — Environment Variables
#
# Copy this file to .env and fill in values as needed.
# .env is gitignored and will not be committed.

# OpenAI API key for text-embedding-3-small.
# Required only when using OpenAI embeddings (group_changes with 2+ changes).
# Single-change grouping works without it.
OPENAI_API_KEY=

# Embedding provider selection.
# Set to "local" to use the offline SHA-256-based LocalEmbeddingProvider
# (no API key needed). Recommended for testing and development.
# Leave unset or remove this line to use OpenAI embeddings in production.
SEGMINT_EMBEDDING_PROVIDER=local
